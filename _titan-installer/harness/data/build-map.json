{
  "_description": "Deterministic build-command lookup for the ?build fast path (answer-cache.py). Generated from titan.config.json repos[] \u2014 generic per-kind defaults only. Per-module build commands are repo-specific detail beyond the shared config's scope; add them by hand under repos.<id>.modules if a repo needs finer-grained matching.",
  "repos": {
    "storefront-ui": {
      "dir": "Acme-Storefront-UI",
      "kind": "aem-maven",
      "default": [
        "cd Acme-Storefront-UI",
        "mvn clean install -DskipTests"
      ],
      "modules": [],
      "notes": [
        "Cloud Manager pipelines"
      ]
    },
    "commerce-layer": {
      "dir": "Acme-Commerce-Integration-Layer",
      "kind": "node-lerna",
      "default": [
        "cd Acme-Commerce-Integration-Layer",
        "yarn install",
        "yarn test"
      ],
      "modules": [],
      "notes": [
        "GraphQL schema",
        "app.config.yaml"
      ]
    },
    "platform-migration": {
      "dir": "Acme-Platform-Migration",
      "kind": "aem-maven",
      "default": [
        "cd Acme-Platform-Migration",
        "mvn clean install -DskipTests"
      ],
      "modules": [],
      "notes": [
        "hybris-api/impl",
        "pim"
      ]
    }
  }
}
