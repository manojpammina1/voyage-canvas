export default function Loading() {
  return (
    <main className="vc-error-shell" aria-live="polite" aria-busy="true">
      <section className="glass-panel vc-error-card">
        <p className="vc-error-card__eyebrow">Royal Caribbean</p>
        <h1>Loading your cruise planner</h1>
        <p>Preparing the assistant workspace.</p>
      </section>
    </main>
  );
}
