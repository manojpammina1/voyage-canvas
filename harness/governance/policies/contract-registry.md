# Contract registry (portable policy)

Cross-repo contracts are declared in `titan.config.json` → `contracts[]`.

Breaking changes require sign-off from all listed owners **before work starts**. Rendered registry table is generated from config at deploy time.

Use `/common/contract-review` (Claude) or consult rendered contract table in AGENTS.md / CLAUDE.md.
